# 第 1 周作业说明

第一周作业已按时提交。当时作业文件放在 `week02/`（由 `week01` 重命名而来），本地保留了空的 `week01/` 目录，Git 不跟踪空文件夹，故远程未显示 `week01`。

本 README 用于占位，便于在仓库中保留 `week01` 目录结构。

**第一周作业内容见：** [`../week02/`](../week02/)（`作业1.py`、`README.md`）
